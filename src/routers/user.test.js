const supertest = require("supertest");
const assert = require('assert');
const user = require('./user');

// describe('POST /user', () => {
//   it('should return user with status 201', (done) => {
//     supertest(user)
//       .POST
//   })
// })